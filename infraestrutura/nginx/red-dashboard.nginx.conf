server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    client_max_body_size 20m;

    include /etc/nginx/redvm-routes/*.conf;

    location / {
        root /var/www/red-portal;
        try_files $uri $uri/ =404;
    }
}
